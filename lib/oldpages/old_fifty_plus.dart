import 'package:flutter/material.dart';

class OldFifty extends StatelessWidget {
  const OldFifty({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Old Fifty"),
      ),
    );
  }
}
