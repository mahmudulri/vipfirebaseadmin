import 'package:flutter/material.dart';

class OldHalfFull extends StatelessWidget {
  const OldHalfFull({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Old Half full"),
      ),
    );
  }
}
