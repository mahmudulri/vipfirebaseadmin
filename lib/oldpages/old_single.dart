import 'package:flutter/material.dart';

class OldSingle extends StatelessWidget {
  const OldSingle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Old Single"),
      ),
    );
  }
}
