import 'package:flutter/material.dart';

class OldSpecial extends StatelessWidget {
  const OldSpecial({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Old Special"),
      ),
    );
  }
}
